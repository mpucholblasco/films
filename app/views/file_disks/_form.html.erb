<%= form_for([@disk, @disk.file_disks.build]) do |f| %>
<p>
	<%= f.label t(:filename) %>
	<br>
	<%= f.text_field :filename %>
</p>
<p>
	<%= f.label t(:size_mb) %>
	<br>
	<%= f.text_field :size_mb %>
</p>
<p>
	<%= f.submit :class => 'button' %>
</p>
<% end %>
